<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CookEase" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Brand Colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary_dark</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/bg_main</item>
        <item name="android:statusBarColor">@color/bg_main</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Components -->
        <item name="materialCardViewStyle">@style/Widget.CookEase.CardView</item>

        <!-- Fallback for TonalButton if not in library version -->
        <item name="buttonStyle">@style/Widget.MaterialComponents.Button</item>
    </style>

    <!-- Custom Tonal Button Style for backward compatibility/missing Material3 styles in XML -->
    <style name="Widget.MaterialComponents.Button.TonalButton" parent="Widget.MaterialComponents.Button.UnelevatedButton">
        <item name="android:backgroundTint">@color/gray_100</item>
        <item name="android:textColor">@color/text_main</item>
        <item name="cornerRadius">16dp</item>
    </style>
</resources>